# from hotel.models import db
# from hotel.models import Room_type,Rooms
# a = Room_type.query.first()
# y = Rooms(203,room_type=a.tid,cost=100,capacity=4,status=0)
# # print(Rooms.query.all())
# db.session.add(y)
# db.session.commit()